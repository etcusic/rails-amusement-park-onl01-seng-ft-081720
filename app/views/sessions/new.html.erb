<%= form_with model: @user do |f| %>
    <%= f.collection_select(:user, User.all, :id, :name, {}, {id: :user_name}) %><br>
    <%= f.label :user_password, "Your password:" %>
    <%= f.text_field :user_password, id: "password" %><br>
    <%= f.submit "Sign In" %>
<% end %>

